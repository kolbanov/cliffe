cs2_moderator_bot_v7_hp_checked

Добавлено:
- команда /hp для всех участников группы;
- ответ показывает хп и предупреждения в формате: у тебя 60хп (2/5 предупреждений).

Сохранено из v6:
- чтение TG_GROUP_CHAT_IDS из .env с поддержкой квадратных скобок;
- /config;
- /connect;
- меню реплик;
- отправка сообщений от лица бота;
- ASU-логика;
- /bite;
- локальное JSON-хранилище.
